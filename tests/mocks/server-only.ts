// No-op stand-in for the 'server-only' package under Vitest, which runs
// in plain Node rather than Next.js's server bundle, where the real
// package would otherwise throw unconditionally.
export {};
